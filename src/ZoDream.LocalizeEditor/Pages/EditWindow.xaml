<Window x:Class="ZoDream.LocalizeEditor.Pages.EditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZoDream.LocalizeEditor.Pages"
        xmlns:ctl="clr-namespace:ZoDream.LocalizeEditor.Controls"
        mc:Ignorable="d"
        Title="编辑" Height="450" Width="800">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Skins/MainSkin.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="40"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <TextBlock Text="源" Style="{StaticResource CenterTb}"/>
            <TextBlock Text="翻译" Style="{StaticResource CenterTb}" Grid.Row="1"/>
            <TextBlock Text="Id" Style="{StaticResource CenterTb}" Grid.Row="2"/>

            <TextBox x:Name="SourceTb" Grid.Column="1" AcceptsReturn="True"/>
            <TextBox x:Name="TargetTb" Grid.Row="1" Grid.Column="1" AcceptsReturn="True"/>
            <TextBox x:Name="IdTb" Grid.Row="2" Grid.Column="1" MaxLines="1"/>

            <Button x:Name="OkBtn" Content="&#xE105;" Grid.Column="2" Style="{StaticResource IconBtn}"/>
        </Grid>
        <Grid x:Name="MorePanel" Grid.Row="1" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="路径" Style="{StaticResource CenterTb}"/>
            <ctl:FileInput x:Name="FileTb" Grid.Column="1"/>

            <TextBlock Text="行号" Grid.Row="1" Style="{StaticResource CenterTb}"/>
            <TextBox x:Name="LineTb" Grid.Row="1" Grid.Column="1" MaxLines="1"/>
        </Grid>
    </Grid>
</Window>
